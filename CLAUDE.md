# arcanea-dashboard-template — Claude Instructions

Read `AGENTS.md` and `.agent-harness.json` first. This is a dashboard template, not a deployed product.

Prefer portable defaults and short, explicit setup notes. Heavy checks belong in commands or CI, never global stop hooks.

